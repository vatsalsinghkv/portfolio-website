import { Sidebar } from '@/components';

import SocialLinks from './SocialLinks';

const Social = () => {
  return (
    <Sidebar>
      <SocialLinks />
    </Sidebar>
  );
};

export default Social;
